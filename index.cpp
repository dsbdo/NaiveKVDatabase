#include<iostream>

using namespace std;
int main(int argc, char* argv[]) {
    // check parameters

    // Get(key) api

    // Put(key, value) 
    
    // Delete(key) put NULL to key api

    // Scan(key1, key2)
    int a = 100;
    int b =100;
    int* const pointer = &a;
    pointer = &b;
    *pointer = 10000;
    cout << *pointer << endl;
    return 0;
}